---
title: Navigation Menu
description: A wrapper for Base UI Navigation Menu — horizontal nav with trigger+content dropdowns, direct link items, hover support, and controlled state.
---

import { NavigationMenuEx } from "@/components/examples";

<NavigationMenuEx />

## Installation

<Tabs className="plain-tabs" items={['CLI', 'Manual']}>

<Tab value="CLI">

```npm
npx shadcn@latest add @glrk-ui/navigation-menu
```

If you haven't set up the prerequisites yet, check out [Prerequest section](/docs/shadcn-wrappers#prerequest).

</Tab>

<Tab value="Manual">

Copy and paste the following code into your project.

<include cwd meta='title="ui/navigation-menu.tsx"'>
  ./components/ui/navigation-menu.tsx
</include>

<include cwd meta='title="ui/navigation-menu-wrapper.tsx"'>
  ./components/ui/navigation-menu-wrapper.tsx
</include>

</Tab>
</Tabs>


## Usage

### Trigger item (with dropdown content)

```tsx
import { NavigationMenuWrapper, NavList } from "@/components/ui/navigation-menu-wrapper"

<NavigationMenuWrapper
  items={[
    {
      key: "products",
      trigger: "Products",
      content: (
        <div className="p-4 w-64">
          <NavList
            items={[
              { href: "/analytics", children: "Analytics" },
              { href: "/reporting", children: "Reporting" },
            ]}
          />
        </div>
      ),
    },
  ]}
/>
```

### Link item (no dropdown)

```tsx
<NavigationMenuWrapper
  items={[
    { key: "pricing", href: "/pricing", children: "Pricing" },
    { key: "blog", href: "/blog", children: "Blog" },
  ]}
/>
```

### Mixed (triggers + links)

```tsx
<NavigationMenuWrapper
  items={[
    {
      key: "home",
      trigger: "Home",
      content: <div className="p-4"><NavList items={[...]} /></div>,
    },
    { key: "pricing", href: "/pricing", children: "Pricing" },
  ]}
/>
```

### Group labels in content

```tsx
{
  key: "platform",
  trigger: "Platform",
  content: (
    <div className="p-3 w-72">
      <p className="mb-1 text-xs font-semibold text-muted-foreground uppercase tracking-wide">
        Tools
      </p>
      <NavList items={[{ href: "/formatter", children: "Formatter" }]} />
      <p className="mt-3 mb-1 text-xs font-semibold text-muted-foreground uppercase tracking-wide">
        Security
      </p>
      <NavList items={[{ href: "/audit", children: "Audit Log" }]} />
    </div>
  ),
}
```

### Default open

```tsx
<NavigationMenuWrapper
  defaultValue="products"
  items={[
    { key: "products", trigger: "Products", content: <div>...</div> },
  ]}
/>
```

### Hover delay (instant open on hover)

Base UI Navigation Menu opens on hover by default (50ms delay). Adjust with `delay` and `closeDelay`:

```tsx
<NavigationMenuWrapper
  delay={0}
  closeDelay={150}
  items={[...]}
/>
```

### Controlled (external open/close)

```tsx
const [value, setValue] = useState<string | null>(null)

<NavigationMenuWrapper
  value={value}
  onValueChange={v => setValue(v)}
  items={[
    { key: "tools", trigger: "Tools", content: <div>...</div> },
  ]}
/>

// Close programmatically:
<button onClick={() => setValue(null)}>Close</button>
// Open specific item:
<button onClick={() => setValue("tools")}>Open Tools</button>
```

### Per-item trigger props

```tsx
{
  key: "products",
  trigger: "Products",
  triggerCls: "font-semibold",
  triggerProps: { disabled: true },
  contentCls: "w-96",
  content: <div>...</div>,
}
```

### NavList helper

```tsx
import { NavList } from "@/components/ui/navigation-menu-wrapper"

<NavList
  wrapperCls="space-y-1"
  items={[
    { href: "/dashboard", children: "Dashboard" },
    { href: "/settings", children: <><SettingsIcon className="size-4" /> Settings</> },
  ]}
/>
```

### NavLinkItem helper

```tsx
import { NavLinkItem } from "@/components/ui/navigation-menu-wrapper"

<NavLinkItem href="/dashboard">Dashboard</NavLinkItem>
```

## Reference

#### NavigationMenuWrapper
<AutoTypeTable
name="NavigationMenuWrapper"
type={`export interface NavigationMenuWrapper {
  /**
   * Array of navigation items.
   *
   * ## Link item
   * \`\`\`tsx
   * { key: "pricing", href: "/pricing", children: "Pricing" }
   * \`\`\`
   *
   * ## Trigger item
   * \`\`\`tsx
   * {
   *   key: "products",
   *   trigger: "Products",
   *   content: <div>dropdown content</div>,
   *   triggerCls?: string,
   *   contentCls?: string,
   *   triggerProps?: Omit<NavigationMenuTrigger.Props, "children" | "className">,
   *   contentProps?: React.ComponentProps<typeof NavigationMenuContent>,
   * }
   * \`\`\`
   */
  items: navMenuItemsT;

  /**
   * Uncontrolled initial open item. Use the item's key value.
   */
  defaultValue?: string | null;

  /**
   * Controlled open item. Set to null to close all.
   */
  value?: string | null;

  /**
   * Callback when the open item changes. Receives null when closed.
   */
  onValueChange?: (value: string | null) => void;

  /**
   * How long to wait before opening on hover (ms).
   * @default 50
   */
  delay?: number;

  /**
   * How long to wait before closing (ms).
   * @default 50
   */
  closeDelay?: number;

  /**
   * Orientation of the navigation menu.
   * @default "horizontal"
   */
  orientation?: "horizontal" | "vertical";

  /**
   * Global CSS class for all trigger buttons.
   */
  triggerCls?: string;

  /**
   * Global props for all trigger buttons (merged with per-item triggerProps).
   */
  triggerProps?: Omit<React.ComponentProps<typeof NavigationMenuTrigger>, "children" | "className">;

  /**
   * Global CSS class for all content panels.
   */
  contentCls?: string;

  /**
   * Content alignment relative to the trigger.
   * @default "start"
   */
  align?: "start" | "center" | "end";

  /**
   * ...rootProps: All NavigationMenu Root props forwarded to the root (openOnHover, loop, orientation, closeDelay, etc.)
   */
  otherProps?: never;
}`}
/>

#### NavList
<AutoTypeTable
name="NavList"
type={`export interface NavList {
  /**
   * Array of navigation link items rendered as an \`<ul>\`.
   *
   * \`\`\`tsx
   * items={[
   *   { href: "/home", children: "Home" },
   *   { href: "/about", children: <><Info className="size-4" /> About</> },
   * ]}
   * \`\`\`
   */
  items: navLinkItemsT;

  /**
   * CSS class applied to the \`<ul>\` wrapper.
   */
  wrapperCls?: string;
}`}
/>

#### NavLinkItem
<AutoTypeTable
name="NavLinkItem"
type={`export interface NavLinkItem {
  /**
   * Content displayed inside the link.
   */
  children: React.ReactNode;

  /**
   * CSS class applied to the link element. Default adds: \`flex items-center gap-2 text-sm\`.
   */
  className?: string;

  /**
   * ...otherProps: All Next.js Link props (href, prefetch, replace, etc.)
   */
  otherProps?: never;
}`}
/>

## Related Components

- [Base UI Navigation Menu](https://base-ui.com/react/components/navigation-menu)
- [Shadcn Navigation Menu](https://ui.shadcn.com/docs/components/navigation-menu)
