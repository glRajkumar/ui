---
title: Sheet
description: A wrapper for Base UI Dialog styled as a side panel, with side support, async loading, nested sheets, and custom trigger composition.
---

import { SheetEx } from "@/components/examples";

<SheetEx />


## Installation

<Tabs className="plain-tabs" items={['CLI', 'Manual']}>

<Tab value="CLI">

```npm
npx shadcn@latest add @glrk-ui/sheet
```

If you haven't set up the prerequisites yet, check out [Prerequest section](/docs/shadcn-wrappers#prerequest).

</Tab>

<Tab value="Manual">

Copy and paste the following code into your project.

<include cwd meta='title="ui/sheet.tsx"'>
  ./components/ui/sheet.tsx
</include>

</Tab>
</Tabs>


## Usage

### Basic

```tsx
import { SheetWrapper } from "@/components/ui/sheet"

export function Basic() {
  return (
    <SheetWrapper
      trigger="Open"
      triggerCls={buttonVariants({ variant: 'outline' })}
      title="Settings"
      description="Adjust your preferences."
      action="Save"
    />
  )
}
```

### Sides

```tsx
<SheetWrapper side="right"  trigger="Right"  title="Right sheet" />
<SheetWrapper side="left"   trigger="Left"   title="Left sheet" />
<SheetWrapper side="top"    trigger="Top"    title="Top sheet" />
<SheetWrapper side="bottom" trigger="Bottom" title="Bottom sheet" />
```

### Hide close button

```tsx
<SheetWrapper
  trigger="Open"
  title="No close button"
  showCloseButton={false}
  action="Done"
/>
```

### Custom element trigger

```tsx
<SheetWrapper
  trigger="Open settings"
  triggerProps={{
    render: <a href="#" />,
    nativeButton: false,
  }}
  triggerCls="text-sm text-primary underline underline-offset-4"
  title="Settings"
/>
```

### Controlled

```tsx
export function Controlled() {
  const [open, setOpen] = useState(false)

  return (
    <>
      <button onClick={() => setOpen(true)}>Open</button>

      <SheetWrapper
        open={open}
        onOpenChange={setOpen}
        title="Controlled sheet"
        action="Save"
        onAction={() => setOpen(false)}
      />
    </>
  )
}
```

### Async action with loading state

```tsx
export function AsyncSave() {
  const [open, setOpen] = useState(false)
  const [loading, setLoading] = useState(false)

  async function handleSave() {
    setLoading(true)
    await saveSettings()
    setLoading(false)
    setOpen(false)
  }

  return (
    <SheetWrapper
      open={open}
      onOpenChange={setOpen}
      trigger="Settings"
      title="Save settings?"
      action="Save"
      loading={loading}
      onAction={handleSave}
    />
  )
}
```

### Nested sheets

```tsx
<SheetWrapper trigger="Open outer" side="right" title="Outer sheet" action="">
  <SheetWrapper
    trigger="Open inner"
    side="left"
    title="Inner sheet"
    description="Slides from the opposite side."
    action="Confirm"
  />
</SheetWrapper>
```

### Custom footer content

```tsx
<SheetWrapper trigger="Open" title="Custom footer" action="" cancel="">
  <SheetFooter>
    <SheetClose render={<Button variant="secondary" className="border" />}>Cancel</SheetClose>
    <Button onClick={handleAction}>Save</Button>
  </SheetFooter>
</SheetWrapper>
```

## Reference

#### SheetFooterWrapper
<AutoTypeTable
name="SheetFooterWrapper"
type={`export interface SheetFooterWrapper {
  /**
   * Text or element for the cancel button. Set to \`""\` or \`null\` to hide.
   * @default "Cancel"
   */
  cancel?: React.ReactNode;

  /**
   * Text or element for the action button. Set to \`""\` or \`null\` to hide.
   */
  action?: React.ReactNode;

  /**
   * When true, disables both buttons and shows a spinner on the action button.
   * Sheet blocks close (Escape, backdrop) while loading.
   * @default false
   */
  loading?: boolean;

  /**
   * Additional CSS classes for the footer container.
   */
  footerCls?: string;

  /**
   * Additional CSS classes for the action button.
   */
  actionCls?: string;

  /**
   * Additional CSS classes for the cancel button.
   */
  cancelCls?: string;

  /**
   * Callback fired when the action button is clicked.
   */
  onAction?: () => void;

  /**
   * Callback fired when the cancel button is clicked.
   */
  onCancel?: () => void;
}`}
/>

#### SheetWrapper
<AutoTypeTable
name="SheetWrapper"
type={`export interface SheetWrapper {
  /**
   * Content rendered inside the auto-wrapped trigger button.
   */
  trigger?: React.ReactNode;

  /**
   * CSS class applied to the trigger element.
   */
  triggerCls?: string;

  /**
   * Additional props for the trigger — handle, payload, render, nativeButton.
   *
   * \`\`\`tsx
   * triggerProps={{ render: <a href="#" />, nativeButton: false }}
   * \`\`\`
   */
  triggerProps?: Omit<SheetPrimitive.Trigger.Props, 'children' | 'className'>;

  /**
   * Sheet title.
   */
  title?: React.ReactNode;

  /**
   * Sheet description. Set to \`""\` to hide.
   */
  description?: React.ReactNode;

  /**
   * Additional content rendered between header and footer.
   */
  children?: React.ReactNode;

  /**
   * Which edge the sheet slides from.
   * @default "right"
   */
  side?: 'top' | 'bottom' | 'right' | 'left';

  /**
   * Whether to show the X close button.
   * @default true
   */
  showCloseButton?: boolean;

  /**
   * Additional CSS classes for the sheet content panel.
   */
  contentCls?: string;

  /**
   * Additional CSS classes for the header section.
   */
  headerCls?: string;

  /**
   * Additional CSS classes for the title.
   */
  titleCls?: string;

  /**
   * Additional CSS classes for the description.
   */
  descriptionCls?: string;

  /**
   * ...otherProps: All SheetFooterWrapper props (cancel, action, loading,
   * footerCls, actionCls, cancelCls, onAction, onCancel) can be passed directly.
   * All Base UI Dialog.Root props (open, onOpenChange, handle, defaultOpen, etc.)
   * are also forwarded to the root.
   */
  otherProps?: never;
}`}
/>

## Related Components

- [Base UI Dialog](https://base-ui.com/react/components/dialog)
- [Shadcn Sheet](https://ui.shadcn.com/docs/components/sheet)
