---
title: Autocomplete
description: A wrapper for Base UI Autocomplete with type-to-filter suggestions, grouped items, async search, inline completion mode, and clear/trigger controls.
---

import { AutocompleteEx } from "@/components/examples";

<AutocompleteEx />

## Installation

<Tabs className="plain-tabs" items={['CLI', 'Manual']}>

<Tab value="CLI">

```npm
npx shadcn@latest add @glrk-ui/autocomplete
```

If you haven't set up the prerequisites yet, check out [Prerequest section](/docs/shadcn-wrappers#prerequest).

</Tab>

<Tab value="Manual">

Copy and paste the following code into your project.

<include cwd meta='title="types/general.d.ts"'>
  ./types/general.d.ts
</include>

<include cwd meta='title="lib/utils.ts"'>
  ./lib/utils.ts
</include>

<include cwd meta='title="ui/autocomplete.tsx"'>
  ./components/ui/autocomplete.tsx
</include>

</Tab>
</Tabs>

## Virtualised Installation

`AutocompleteVirtualisedWrapper` renders large flat lists efficiently using `@tanstack/react-virtual`. Install separately — grouping is not supported.

<Tabs className="plain-tabs" items={['CLI', 'Manual']}>

<Tab value="CLI">

```npm
npx shadcn@latest add @glrk-ui/autocomplete-virtual
```

</Tab>

<Tab value="Manual">

<include cwd meta='title="ui/autocomplete-virtualised.tsx"'>
  ./components/ui/autocomplete-virtualised.tsx
</include>

</Tab>
</Tabs>


## Usage

### Basic

```tsx
import { AutocompleteWrapper } from "@/components/ui/autocomplete"

<AutocompleteWrapper
  items={["Apple", "Banana", "Orange"]}
  placeholder="Search fruits..."
  className="w-64"
/>
```

### With clear and trigger buttons

```tsx
<AutocompleteWrapper
  items={items}
  placeholder="Search..."
  showClear
  showTrigger
  className="w-64"
/>
```

### Controlled

```tsx
const [value, setValue] = useState("")

<AutocompleteWrapper
  items={items}
  value={value}
  onValueChange={setValue}
  placeholder="Search..."
  showClear
/>
```

### Item objects

```tsx
const frameworks = [
  { value: "next", label: "Next.js" },
  { value: "remix", label: "Remix" },
  { value: "astro", label: "Astro" },
]

<AutocompleteWrapper items={frameworks} placeholder="Select framework..." />
```

### Grouped items

```tsx
const grouped = [
  {
    group: "Frontend",
    items: [
      { value: "react", label: "React" },
      { value: "vue", label: "Vue" },
    ],
  },
  {
    group: "Backend",
    items: [
      { value: "node", label: "Node.js" },
      { value: "go", label: "Go" },
    ],
  },
]

<AutocompleteWrapper items={grouped} placeholder="Search tech..." showClear />
```

### Limit results

```tsx
<AutocompleteWrapper items={items} placeholder="Search..." limit={5} />
```

### Inline mode

Completes input text inline as the user types:

```tsx
<AutocompleteWrapper items={items} mode="inline" placeholder="Start typing..." />
```

### Custom status and empty slots

Override the default loading/empty UI with `renderStatus` and `renderEmpty`:

```tsx
<AutocompleteWrapper
  items={items}
  renderStatus={isFetching
    ? <p className="flex items-center gap-2 p-2 text-sm"><Loader2 className="size-4 animate-spin" /> Fetching...</p>
    : null
  }
  renderEmpty={<p className="py-6 text-center text-sm">Nothing here yet</p>}
/>
```

### Async search

Disable client-side filter with `filter={null}` and supply results externally:

```tsx
const [inputValue, setInputValue] = useState("")
const [results, setResults] = useState<string[]>([])
const [isFetching, setIsFetching] = useState(false)
const timerRef = useRef<ReturnType<typeof setTimeout> | null>(null)

function handleValueChange(val: string) {
  setInputValue(val)
  if (timerRef.current) clearTimeout(timerRef.current)
  if (!val.trim()) { setResults([]); return }
  setIsFetching(true)
  timerRef.current = setTimeout(async () => {
    setResults(await fetchSuggestions(val))
    setIsFetching(false)
  }, 400)
}

<AutocompleteWrapper
  items={results}
  value={inputValue}
  onValueChange={handleValueChange}
  filter={null}
  placeholder="Search..."
  showClear
  renderStatus={isFetching
    ? <p className="flex items-center gap-2 p-2 text-sm"><Loader2 className="size-4 animate-spin" /> Fetching...</p>
    : null
  }
  renderEmpty={<p className="py-6 text-center text-sm">No results found</p>}
/>
```

### Virtualised — large lists

Use `AutocompleteVirtualisedWrapper` for lists with hundreds or thousands of items:

```tsx
import { AutocompleteVirtualisedWrapper } from "@/components/ui/autocomplete-virtualised"

<AutocompleteVirtualisedWrapper
  items={largeList}
  placeholder="Search 1000 items…"
  className="w-64"
  showClear
/>
```

Custom row height and overscan:

```tsx
<AutocompleteVirtualisedWrapper
  items={largeList}
  placeholder="Search…"
  maxHeight={200}
  estimateSize={36}
  virtualizerOptions={{ overscan: 10 }}
/>
```

## Reference

<AutoTypeTable
name="AutocompleteWrapperProps"
type={`export type AutocompleteWrapperProps = {
  /**
   * Suggestion items. Accepts strings, itemT objects ({ value, label }),
   * or groupT objects ({ group, items[] }).
   */
  items?: itemsT;

  /**
   * CSS class applied to the input group (the visible input container).
   */
  className?: string;

  /**
   * Input placeholder text.
   */
  placeholder?: string;

  /**
   * Show loading state in the empty slot.
   * @default false
   */
  isLoading?: boolean;

  /**
   * Text shown when no items match. Ignored when isLoading is true.
   * @default "No results found"
   */
  emptyMessage?: string;

  /**
   * Show a clear (x) button inside the input.
   * @default false
   */
  showClear?: boolean;

  /**
   * Show a chevron trigger button to toggle the popup.
   * @default true
   */
  showTrigger?: boolean;

  /**
   * CSS class applied to the popup content.
   */
  contentCls?: string;

  /**
   * CSS class applied to each suggestion item.
   */
  itemCls?: string;

  /**
   * CSS class applied to each group container.
   */
  groupCls?: string;

  /**
   * Props forwarded to the native input element.
   */
  inputProps?: Omit<React.ComponentProps<'input'>, 'value' | 'onChange'>;

  /**
   * Custom React node rendered inside the Status slot.
   * When provided, replaces the default isLoading spinner entirely.
   */
  renderStatus?: React.ReactNode;

  /**
   * Custom React node rendered inside the Empty slot.
   * When provided, replaces the default emptyMessage entirely.
   */
  renderEmpty?: React.ReactNode;

  // All Autocomplete.Root props are forwarded:
  // value, onValueChange, open, onOpenChange, mode, inline,
  // autoHighlight, limit, filter, itemToStringValue,
  // disabled, readOnly, etc.
}`}
/>

## Virtualised Reference

`AutocompleteVirtualisedWrapper` accepts all the same props as `AutocompleteWrapper` except `groupCls`. The following props are unique to the virtualised variant:

<AutoTypeTable
name="AutocompleteVirtualisedWrapperProps"
type={`export type AutocompleteVirtualisedWrapperProps = {
  /**
   * Estimated row height in pixels used by the virtualizer.
   * @default 32
   */
  estimateSize?: number;

  /**
   * Maximum height of the scrollable list in pixels.
   * @default 300
   */
  maxHeight?: number;

  /**
   * Extra options forwarded to useVirtualizer (except count and getScrollElement).
   * Useful for tuning overscan, measureElement, etc.
   */
  virtualizerOptions?: Partial<Omit<VirtualizerOptions<HTMLDivElement, Element>, "count" | "getScrollElement">>;
}`}
/>

## Related Components

- [Base UI Autocomplete](https://base-ui.com/react/components/autocomplete)
