---
title: Combobox
description: A wrapper for Base UI Combobox with single and multi-select, chips, grouped options, async loading, custom label rendering, and create-new support.
---

import { ComboboxEx } from "@/components/examples";

<ComboboxEx />

## Installation

<Tabs className="plain-tabs" items={['CLI', 'Manual']}>

<Tab value="CLI">

```npm
npx shadcn@latest add @glrk-ui/combobox
```

If you haven't set up the prerequisites yet, check out [Prerequest section](/docs/shadcn-wrappers#prerequest).

</Tab>

<Tab value="Manual">

Copy and paste the following code into your project.

<include cwd meta='title="types/general.d.ts"'>
  ./types/general.d.ts
</include>

<include cwd meta='title="lib/utils.ts"'>
  ./lib/utils.ts
</include>

<include cwd meta='title="ui/combobox.tsx"'>
  ./components/ui/combobox.tsx
</include>

</Tab>
</Tabs>

## Virtualised Installation

`ComboboxVirtualisedWrapper` renders large flat lists efficiently using `@tanstack/react-virtual`. Install separately — grouping is not supported.

<Tabs className="plain-tabs" items={['CLI', 'Manual']}>

<Tab value="CLI">

```npm
npx shadcn@latest add @glrk-ui/combobox-virtual
```

</Tab>

<Tab value="Manual">

<include cwd meta='title="ui/combobox-virtualised.tsx"'>
  ./components/ui/combobox-virtualised.tsx
</include>

</Tab>
</Tabs>


## Usage

### Basic

```tsx
import { ComboboxWrapper } from "@/components/ui/combobox"

<ComboboxWrapper
  items={["Apple", "Banana", "Orange"]}
  placeholder="Search fruits..."
/>
```

### Multiple selection (chips)

Pass `multiple` to enable chip-based multi-select:

```tsx
<ComboboxWrapper
  multiple
  items={["Apple", "Banana", "Orange"]}
  placeholder="Search fruits..."
/>
```

### Options format

`items` accepts primitives, option objects, groups, and separators — mixed freely:

```tsx
const items = [
  "Simple string",
  42,
  "---",                                             // separator
  { label: "Custom label", value: "custom" },
  { label: <><SunIcon /> Light</>, value: "light" }, // ReactNode label
  { label: "Disabled", value: "off", disabled: true },
  {
    group: "Category",
    options: ["Option A", "Option B"],
  },
]

<ComboboxWrapper items={items} placeholder="Pick one" />
```

### Grouped options

```tsx
<ComboboxWrapper
  items={[
    { group: "Fruits", options: ["Apple", "Banana"] },
    { group: "Veggies", options: ["Carrot", "Broccoli"] },
  ]}
  placeholder="Select food"
/>
```

### Trigger and clear buttons

```tsx
<ComboboxWrapper items={items} showTrigger showClear placeholder="Search" />
<ComboboxWrapper items={items} showTrigger={false} placeholder="No chevron" />
```

### Indicator placement

```tsx
<ComboboxWrapper items={items} indicatorAt="left" placeholder="Left check" />
<ComboboxWrapper items={items} indicatorAt="right" placeholder="Right check" /> {/* default */}
```

### Disabled item

```tsx
<ComboboxWrapper
  items={[
    { label: "Available", value: "a" },
    { label: "Unavailable", value: "b", disabled: true },
  ]}
  placeholder="Select"
/>
```

### Loading state

```tsx
<ComboboxWrapper items={[]} isLoading placeholder="Search..." />
```

Shows "Loading..." in the empty slot while `isLoading` is true.

### Empty message

```tsx
<ComboboxWrapper items={items} emptyMessage="Nothing matched" placeholder="Search" />
```

### Controlled value

Single:

```tsx
const [value, setValue] = useState<allowedPrimitiveT>("")

<ComboboxWrapper
  items={items}
  value={value}
  onValueChange={setValue}
/>
```

Multiple:

```tsx
const [values, setValues] = useState<allowedPrimitiveT[]>([])

<ComboboxWrapper
  multiple
  items={items}
  value={values}
  onValueChange={setValues}
/>
```

### Controlled query and open state

```tsx
const [query, setQuery] = useState("")
const [open, setOpen] = useState(false)

<ComboboxWrapper
  items={items}
  query={query}
  onQueryChange={setQuery}
  open={open}
  onOpenChange={setOpen}
/>
```

### Async options

Combine controlled query with async fetch:

```tsx
const [query, setQuery] = useState("")
const { data: items = [], isLoading } = useQuery({
  queryKey: ["users", query],
  queryFn: () => fetchUsers(query),
})

<ComboboxWrapper
  items={items}
  isLoading={isLoading}
  query={query}
  onQueryChange={setQuery}
  placeholder="Search users..."
/>
```

### Async search

Disable client filtering with `filter={() => true}` and update items externally on input change:

```tsx
const [results, setResults] = useState<string[]>([])
const [isFetching, setIsFetching] = useState(false)
const timerRef = useRef<ReturnType<typeof setTimeout> | null>(null)

function handleInputChange(query: string) {
  if (timerRef.current) clearTimeout(timerRef.current)
  if (!query.trim()) { setResults([]); return }
  setIsFetching(true)
  timerRef.current = setTimeout(async () => {
    setResults(await fetchOptions(query))
    setIsFetching(false)
  }, 400)
}

<ComboboxWrapper
  items={results}
  filter={() => true}
  onInputValueChange={handleInputChange}
  placeholder="Type to search…"
  showClear
  renderStatus={isFetching
    ? <p className="flex items-center gap-2 p-2 text-sm"><Loader2 className="size-4 animate-spin" /> Fetching...</p>
    : null
  }
  renderEmpty={<p className="py-6 text-center text-sm">No results found</p>}
/>
```

### Custom status and empty slots

Override the default loading/empty UI with `renderStatus` and `renderEmpty`:

```tsx
<ComboboxWrapper
  items={items}
  isLoading={isLoading}
  renderStatus={isLoading ? <MySpinner /> : null}
  renderEmpty={<p className="py-6 text-center">Nothing here yet</p>}
/>
```

### Custom chip label (renderValue)

Used in `multiple` mode — controls what renders inside each chip. Receives the raw value string and the matched option:

```tsx
<ComboboxWrapper
  multiple
  items={[
    { label: <><SunIcon /> Light</>, value: "light" },
    { label: <><MoonIcon /> Dark</>, value: "dark" },
  ]}
  renderValue={(value, option) => (
    <span className="flex items-center gap-1">
      {value === "light" ? <SunIcon className="size-3" /> : <MoonIcon className="size-3" />}
      {value}
    </span>
  )}
/>
```

### Custom input label (getItemLabel)

Controls the string shown in the input after selection (single mode). Useful when option labels are ReactNodes:

```tsx
<ComboboxWrapper
  items={[
    { label: <><SunIcon /> Light</>, value: "light" },
    { label: <><MoonIcon /> Dark</>, value: "dark" },
  ]}
  getItemLabel={(value) => value === "light" ? "Light mode" : "Dark mode"}
/>
```

> Without `getItemLabel`, the wrapper uses `extractText` to derive a string from ReactNode labels automatically.

### Create new option

Use `hideList` to hide the dropdown list (render your own "Create" item), and wire up `onValueChange` to extend items:

```tsx
const [items, setItems] = useState(["Apple", "Banana"])
const [query, setQuery] = useState("")

const hasMatch = items.some(
  (i) => String(i).toLowerCase() === query.toLowerCase()
)

<ComboboxWrapper
  items={items}
  query={query}
  onQueryChange={setQuery}
  onValueChange={(v) => {
    if (!items.includes(v as string)) setItems((prev) => [...prev, v as string])
  }}
  hideList={hasMatch || !query}
  placeholder="Type to create..."
/>
```

### Additional input props

Pass native input attributes via `inputProps`:

```tsx
<ComboboxWrapper
  items={items}
  inputProps={{ autoComplete: "off", maxLength: 50 }}
/>
```

### Custom styling

```tsx
<ComboboxWrapper
  items={items}
  placeholder="Styled"
  triggerCls="w-72"
  contentCls="max-h-48"
  groupCls="pb-0"
  itemCls="text-xs"
/>
```

### Virtualised — large lists

Use `ComboboxVirtualisedWrapper` for lists with hundreds or thousands of items:

```tsx
import { ComboboxVirtualisedWrapper } from "@/components/ui/combobox-virtualised"

<ComboboxVirtualisedWrapper
  items={largeList}
  placeholder="Search 1000 options…"
  triggerCls="w-64"
  showClear
/>
```

Custom row height and overscan:

```tsx
<ComboboxVirtualisedWrapper
  items={largeList}
  placeholder="Search…"
  maxHeight={200}
  estimateSize={36}
  virtualizerOptions={{ overscan: 10 }}
/>
```

## Reference

<AutoTypeTable
name="ComboboxWrapper"
type={`export interface ComboboxWrapper {
  /**
   * Array of options. Supports primitives, option objects, groups, and separators.
   *
   * - Primitives: string | number | boolean
   * - Option object: { label: ReactNode, value: string | number | boolean, className?: string, disabled?: boolean }
   * - Group: { group: string, options: Array<...>, className?: string }
   * - Separator: use the string "---"
   */
  items?: optionsT;

  /**
   * Enable multi-select with chip-based input.
   * @default false
   */
  multiple?: boolean;

  /**
   * Placeholder text for the search input.
   */
  placeholder?: string;

  /**
   * Message shown when no items match the search query.
   * @default "No options found"
   */
  emptyMessage?: string;

  /**
   * Show a loading indicator in the empty slot while data is being fetched.
   * @default false
   */
  isLoading?: boolean;

  /**
   * Position of the checkmark indicator inside each item.
   * @default "right"
   */
  indicatorAt?: "left" | "right";

  /**
   * Show the chevron trigger button.
   * @default true
   */
  showTrigger?: boolean;

  /**
   * Show the clear button.
   * @default false
   */
  showClear?: boolean;

  /**
   * Hide the dropdown list — useful for "create new" patterns where
   * you render your own item outside the standard list.
   * @default false
   */
  hideList?: boolean;

  /**
   * Custom render function for chip labels in multiple mode.
   * Receives the raw value string and the matched option (if any).
   *
   * If omitted, the option label (ReactNode) is used directly.
   *
   * \`\`\`tsx
   * renderValue={(value, option) => <Badge>{value}</Badge>}
   * \`\`\`
   */
  renderValue?: (value: string, option: allowedPrimitiveT | optionT | undefined) => React.ReactNode;

  /**
   * Override the string shown in the input after selection (single mode).
   * Receives the selected value string; must return a plain string.
   *
   * If omitted, the wrapper derives a string from ReactNode labels via extractText.
   *
   * \`\`\`tsx
   * getItemLabel={(value) => myLookup[value] ?? value}
   * \`\`\`
   */
  getItemLabel?: (value: string) => string;

  /**
   * Additional props forwarded to the underlying <input> element.
   */
  inputProps?: React.ComponentProps<"input">;

  /**
   * CSS classes applied to the trigger input (or chips container in multiple mode).
   */
  triggerCls?: string;

  /**
   * CSS classes applied to the popup content.
   */
  contentCls?: string;

  /**
   * CSS classes applied to each group wrapper.
   */
  groupCls?: string;

  /**
   * CSS classes applied to each option item.
   */
  itemCls?: string;

  /**
   * Custom React node rendered inside the Status slot.
   * When provided, replaces the default isLoading spinner entirely.
   */
  renderStatus?: React.ReactNode;

  /**
   * Custom React node rendered inside the Empty slot.
   * When provided, replaces the default emptyMessage entirely.
   */
  renderEmpty?: React.ReactNode;

  /**
   * ...rootProps: All Base UI Combobox.Root props (value, onValueChange, defaultValue,
   * query, onQueryChange, open, onOpenChange, disabled, name, etc.) are forwarded.
   */
  otherProps?: never;
}`}
/>

## Virtualised Reference

`ComboboxVirtualisedWrapper` accepts all the same props as `ComboboxWrapper` except `multiple`, `hideList`, `groupCls`, and `renderValue`. The following props are unique to the virtualised variant:

<AutoTypeTable
name="ComboboxVirtualisedWrapperProps"
type={`export type ComboboxVirtualisedWrapperProps = {
  /**
   * Estimated row height in pixels used by the virtualizer.
   * @default 32
   */
  estimateSize?: number;

  /**
   * Maximum height of the scrollable list in pixels.
   * @default 300
   */
  maxHeight?: number;

  /**
   * Extra options forwarded to useVirtualizer (except count and getScrollElement).
   * Useful for tuning overscan, measureElement, etc.
   */
  virtualizerOptions?: Partial<Omit<VirtualizerOptions<HTMLDivElement, Element>, "count" | "getScrollElement">>;
}`}
/>

## Related Components

- [Base UI Combobox](https://base-ui.com/react/components/combobox)
