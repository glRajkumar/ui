---
title: Dialog
description: A simplified wrapper for Shadcn Dialog with flexible option handling and automatic type conversion
---

import { DialogEx } from "@/components/examples";

<DialogEx />


## Installation

<Tabs className="plain-tabs" items={['CLI', 'Manual', 'Full Guide']}>

<Tab value="CLI">

```npm
npx shadcn@latest add @glrk-ui/dialog
```

</Tab>

<Tab value="Manual">

Copy and paste the following code into shadcn `dialog` component.

```tsx title="ui/dialog.tsx"
type DialogFooterWrapperProps = {
  cancel?: React.ReactNode
  action?: React.ReactNode
  footerCls?: string
  actionCls?: string
  cancelCls?: string
  onAction?: () => void
  onCancel?: () => void
}

function DialogFooterWrapper({
  cancel,
  action,
  footerCls,
  actionCls,
  cancelCls,
  onAction = () => { },
  onCancel = () => { },
}: DialogFooterWrapperProps) {
  return (
    <DialogFooter className={footerCls}>
      {
        cancel &&
        <DialogClose asChild>
          <Button
            variant="secondary"
            onClick={onCancel}
            className={cn("border", cancelCls)}
            asChild={typeof cancel !== "string"}
          >
            {cancel}
          </Button>
        </DialogClose>
      }

      {
        action &&
        <Button
          onClick={onAction}
          className={actionCls}
          asChild={typeof action !== "string"}
        >
          {action}
        </Button>
      }
    </DialogFooter>
  )
}

type DialogWrapperProps = {
  title?: React.ReactNode
  trigger?: React.ReactNode
  children?: React.ReactNode
  description?: React.ReactNode
  descriptionCls?: string
  contentCls?: string
  headerCls?: string
  titleCls?: string
} & DialogFooterWrapperProps

function DialogWrapper({
  trigger,
  title,
  description,
  children,
  contentCls,
  headerCls,
  titleCls,
  descriptionCls,

  cancel = "Cancel",
  action,
  footerCls,
  actionCls,
  cancelCls,
  onAction,
  onCancel,
  ...props
}: React.ComponentProps<typeof DialogPrimitive.Root> & DialogWrapperProps) {
  return (
    <Dialog {...props}>
      {trigger &&
        <DialogTrigger asChild={typeof trigger !== "string"}>{trigger}</DialogTrigger>
      }

      <DialogContent className={contentCls}>
        <DialogHeader className={headerCls}>
          <DialogTitle className={titleCls}>{title}</DialogTitle>
          {description && (
            <DialogDescription className={descriptionCls}>
              {description}
            </DialogDescription>
          )}
        </DialogHeader>

        {children}

        {
          (!!cancel || !!action) &&
          <DialogFooterWrapper
            cancel={cancel}
            action={action}
            footerCls={footerCls}
            actionCls={actionCls}
            cancelCls={cancelCls}
            onAction={onAction}
            onCancel={onCancel}
          />
        }
      </DialogContent>
    </Dialog>
  )
}
```

Add `DialogWrapper` and `DialogFooterWrapper` at the export block.

```tsx
export {
  Dialog,
  DialogPortal,
  DialogOverlay,
  DialogTrigger,
  DialogContent,
  DialogHeader,
  DialogFooter,
  DialogTitle,
  DialogDescription,
  DialogAction,
  DialogClose,
/* [!code ++:2] [!code focus:2] */
  DialogWrapper,
  DialogFooterWrapper,
}
```

</Tab>

<Tab value="Full Guide">
<section id="setup-section">
<div className="fd-steps [&_h3]:fd-step">

### Installation

```npm
npm install @radix-ui/react-dialog
```

### dialog.tsx
Copy and paste the following code into your project.

<include cwd meta='title="ui/dialog.tsx"'>
  ./components/ui/dialog.tsx
</include>

### Done
You can now use `DialogWrapper`

</div>
</section>
</Tab>
</Tabs>


## Usage

### Basic
```tsx
import { DialogWrapper } from "@/components/ui/dialog";
import { Button } from "@/components/ui/button";

export function Basic() {
  return (
    <>
      <DialogWrapper
        trigger={<Button>Delete</Button>}
        title="Are you absolutely sure?"
        description="This action cannot be undone. This will permanently delete your account and remove your data from our servers."
      />
    </>
  )
}
```

### Controlled
```tsx
import { useState } from "react"
import { DialogWrapper } from "@/components/ui/dialog";
import { Button } from "@/components/ui/button";

export function Controlled() {
  const [open, setOpen] = useState(false)

  return (
    <>
      <DialogWrapper
        open={open}
        onOpenChange={setOpen}
        trigger={<Button>Controlled</Button>}
        onAction={() => setOpen(false)}
        title="Are you absolutely sure?"
        description="This action cannot be undone. This will permanently delete your account and remove your data from our servers."
      />
    </>
  )
}

// Programaticaly open model without trigger
export function Controlled2() {
  const [open, setOpen] = useState(false)
 
  const updateOpen = () => setOpen(p => !p)

  return (
    <>
      <Button onClick={updateOpen}>Controlled 2</Button>

      <DialogWrapper
        open={open}
        onOpenChange={updateOpen}
        onAction={updateOpen}
        title="Are you absolutely sure?"
        description="This action cannot be undone. This will permanently delete your account and remove your data from our servers."
      />
    </>
  )
}
```

### Custom
```tsx
export function Custom() {
  return (
    <DialogWrapper
      trigger={<Button>Delete</Button>}
      title="Some Custom Modal"

      // to remove footer make cancel empty
      cancel=""
    >
      <div>...</div>
    </DialogWrapper>
  )
}
```

## Reference

#### DialogFooterWrapper
<AutoTypeTable 
name="DialogFooterWrapper" 
type={`export interface DialogFooterWrapper {
    /**
   * Text or element for the cancel button
   * Set to \`""\` or \`null\` or \`false\` to hide the cancel button
   * 
   * @default "Cancel"
   * 
   * \`\`\`tsx
   * cancel="No, keep it"
   * // or custom element
   * cancel={<button><XIcon />Nevermind</button>}
   * // or hide it
   * cancel={null}
   * \`\`\`
   */
  cancel?: React.ReactNode;

  /**
   * Text or element for the confirm/action button
   * Set to \`""\` or \`null\` or \`false\` to hide the action button
   * 
   * \`\`\`tsx
   * action="Yes, delete"
   * // or custom element
   * action={<button><TrashIcon />Delete Forever</button>}
   * \`\`\`
   */
  action?: React.ReactNode;

  /**
   * Additional CSS classes for the footer container
   */
  footerCls?: string;

  /**
   * Additional CSS classes for the action/confirm button
   * 
   * \`\`\`tsx
   * actionCls="bg-red-600 hover:bg-red-700"
   * \`\`\`
   */
  actionCls?: string;

  /**
   * Additional CSS classes for the cancel button
   */
  cancelCls?: string;

  /**
   * Callback fired when the action/confirm button is clicked
   * Dialog remains open after this callback - close it via state if needed
   * 
   * \`\`\`tsx
   * onAction={() => {
   *   deleteAccount()
   *   setOpen(false)
   * }}
   * \`\`\`
   */
  onAction?: () => void;

  /**
   * Callback fired when the cancel button is clicked
   * Dialog automatically closes after this callback
   * 
   * \`\`\`tsx
   * onCancel={() => {
   *   console.log("User cancelled")
   * }}
   * \`\`\`
   */
  onCancel?: () => void;
}`} 
/>

#### DialogWrapper
<AutoTypeTable 
name="DialogWrapper" 
type={`export interface DialogWrapper {
  /**
   * Optional trigger element that opens the dialog when clicked
   * If not provided, control the dialog via the \`open\` prop
   * 
   * Automatically uses \`asChild\` when React element is provided
   * 
   * \`\`\`tsx
   * trigger={<Button variant="destructive">Delete Account</Button>}
   * // or
   * trigger="Click to open"
   * \`\`\`
   */
  trigger?: React.ReactNode;

  /**
   * Dialog title displayed in the header
   * 
   * \`\`\`tsx
   * title="Delete User Account"
   * // or
   * title={<span className="text-red-600">Warning!</span>}
   * \`\`\`
   */
  title?: React.ReactNode;

  /**
   * Description text explaining the action's consequences
   * Set to \`"null"\` or \`undefined\` to hide the description
   * 
   * \`\`\`tsx
   * description="All files and data will be permanently deleted."
   * // or hide it
   * description={null}
   * \`\`\`
   */
  description?: React.ReactNode;

  /**
   * Additional content rendered between description and footer
   * Use for custom forms, warnings, or additional information
   * 
   * \`\`\`tsx
   * <DialogWrapper>
   *   <div className="my-4">
   *     <input placeholder="Type 'DELETE' to confirm" />
   *   </div>
   * </DialogWrapper>
   * \`\`\`
   */
  children?: React.ReactNode;

  /**
   * Additional CSS classes for the dialog content container
   */
  contentCls?: string;

  /**
   * Additional CSS classes for the header section
   */
  headerCls?: string;

  /**
   * Additional CSS classes for the title
   */
  titleCls?: string;

  /**
   * Additional CSS classes for the description
   */
  descriptionCls?: string;

 /**
   * ...otherProps: All properties from Radix UI Dialog Root can be passed directly.
   */
  otherProps: DialogPrimitive.Root
}`} 
/>

Properties of `DialogFooterWrapper` can be passed directly.

## Related Components

- [Shadcn Dialog](https://ui.shadcn.com/docs/components/dialog)
- [Radix UI Dialog](https://www.radix-ui.com/primitives/docs/components/dialog)
