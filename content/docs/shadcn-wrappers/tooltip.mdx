---
title: Tooltip
description: A simplified wrapper for Shadcn Tooltip with flexible option handling and automatic type conversion
---

import { TooltipEx } from "@/components/examples";

<TooltipEx />

## Installation

<Tabs className="plain-tabs" items={['CLI', 'Manual']}>

<Tab value="CLI">

```npm
npx shadcn@latest add @glrk-ui/tooltip
```

If you haven't set up the prerequisites yet, check out [Prerequest section](/docs/shadcn-wrappers#prerequest).

</Tab>

<Tab value="Manual">

Copy and paste the following code into your project.

<include cwd meta='title="ui/tooltip.tsx"'>
  ./components/ui/tooltip.tsx
</include>

</Tab>
</Tabs>


## Usage

### Basic
```tsx
import { TooltipWrapper } from "@/components/ui/tooltip";

export function Basic() {
  return (
     <TooltipWrapper
      trigger="Hover"
      content="Some tooltip content"
    />
  )
}
```

## Reference

#### TooltipWrapper
<AutoTypeTable 
name="TooltipWrapper" 
type={`export interface TooltipWrapper {
    /**
   * Content displayed in the clickable trigger button
   * Can be a string or any React element
   */
  trigger: React.ReactNode;

  /**
   * content of the tooltip. 
   */
  content: React.ReactNode;

  /**
   * Optional CSS classes for this specific accordion trigger
   */
  triggerCls?: string;

  /**
   * Additional CSS classes for the content
   */
  contentCls?: string;

  /**
   * ...contentProps: All properties from Base UI Tooltip Content can be passed directly.
   */
  contentProps: TooltipPrimitive.Content;

  /**
   * ...otherProps: All properties from Base UI Tooltip Root can be passed directly.
   */
  otherProps: TooltipPrimitive.Root
}`} 
/>

## Related Components

- [Shadcn Tooltip](https://ui.shadcn.com/docs/components/tooltip)
- [Base UI Tooltip](https://base-ui.com/react/components/tooltip)
