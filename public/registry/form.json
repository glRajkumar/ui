{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "form",
  "title": "Form",
  "description": "Base UI Form wrapper with server error propagation, JS object submission via onFormSubmit, and imperative validation via actionsRef.",
  "files": [
    {
      "path": "components/ui/form.tsx",
      "content": "'use client'\n\nimport { Form as FormPrimitive } from '@base-ui/react/form'\n\nimport { cn } from '@/lib/utils'\n\nfunction Form({ className, ...props }: React.ComponentProps<typeof FormPrimitive>) {\n  return (\n    <FormPrimitive data-slot=\"form\" className={cn('flex flex-col gap-4', className)} {...props} />\n  )\n}\n\nexport { Form }\n",
      "type": "registry:ui"
    }
  ],
  "type": "registry:ui"
}